# behavior_tree_ros
Base library for behavior tree plugins development (BehaviorTree.CPP library) and generic tree launcher node.

## Dependencies

* **ros-type-introspection**
```bash
sudo apt-get install ros-$ROS_DISTRO-ros-type-introspection
```
